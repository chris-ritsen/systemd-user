[Unit]

Description=Clipboard Sync Service
After=network.target

[Service]

Environment = DISPLAY=:0
Restart=always
RestartSec=10
Type=simple

ExecStart=%h/.local/bin/clipboard-sync --redis-host 127.0.0.1

[Install]

WantedBy=default.target
